<mj-section>
    @for (int i = 0; i < NumberOfSectionColumns; i++)
    {
        <mj-column width="@($"{100m / (decimal)NumberOfSectionColumns}%")">
            <WidgetZone ZoneName="@($"Widget_Zone_{i}")" />
        </mj-column>
    }
</mj-section>

@code {
    /// <summary>
    /// The number of columns in a section.
    /// </summary>
    [Parameter]
    public int NumberOfSectionColumns { get; set; }
}